subprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'

    repositories {
       mavenCentral()
    }

    version = '1.0'

    jar {
        manifest.attributes provider: 'RTF2 Ingenieria Web'
    }
}